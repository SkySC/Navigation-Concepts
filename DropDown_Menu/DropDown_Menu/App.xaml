<Application x:Class="DropDown_Menu.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:DropDown_Menu"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <SolidColorBrush x:Key="TopButton" Color="#70146A"/>
        <SolidColorBrush x:Key="ChildButton" Color="#A0629C"/>


        <Style TargetType="Button" x:Key="TopButtonStyle">
            <Setter Property="Background" Value="{StaticResource TopButton}"/>
            <Setter Property="Foreground" Value="Wheat"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="BorderBrush" Value="{StaticResource TopButton}"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="15"/>
        </Style>



        <Style TargetType="Button" x:Key="ChildButtonStyle" BasedOn="{StaticResource TopButtonStyle}">
            <Setter Property="Background" Value="{StaticResource ChildButton}"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="0"/>

        </Style>
    

    </Application.Resources>
</Application>
